name = "extrator-leads-google"
compatibility_date = "2024-01-01"
pages_build_output_dir = "cloudflare/public"

# Pages Functions auto-detectadas em cloudflare/functions/
# Secrets (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, etc.)
# configurados via: npx wrangler pages secret bulk
# NUNCA colocar secrets neste arquivo.
